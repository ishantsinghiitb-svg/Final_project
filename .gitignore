# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-safari
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars
# Local-only runtime vars (real ADMIN_EMAILS/GOOGLE_CLIENT_ID/redirect URI —
# see wrangler.jsonc.example for the committed template). Merged into the
# generated wrangler config at build time; must never be committed.
wrangler.json
wrangler.jsonc
wrangler.toml

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env
