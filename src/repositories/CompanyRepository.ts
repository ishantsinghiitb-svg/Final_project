// Placeholder — methods will be implemented in Phase 3+
export class CompanyRepository {
  // TODO: findById(id: string): Promise<Company | null>
  // TODO: findByName(name: string): Promise<Company | null>
  // TODO: create(company: Partial<Company>): Promise<Company | null>
  // TODO: update(id: string, updates: Partial<Company>): Promise<Company | null>
}
