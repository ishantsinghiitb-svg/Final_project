// Placeholder — methods will be implemented in Phase 3+
export class JobRepository {
  // TODO: findById(id: string): Promise<GlobalJob | null>
  // TODO: findByUser(userId: string): Promise<GlobalJob[]>
  // TODO: create(job: Partial<GlobalJob>): Promise<GlobalJob | null>
  // TODO: update(id: string, updates: Partial<GlobalJob>): Promise<GlobalJob | null>
  // TODO: delete(id: string): Promise<void>
}
