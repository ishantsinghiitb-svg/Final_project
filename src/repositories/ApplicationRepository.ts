// Placeholder — methods will be implemented in Phase 3+
export class ApplicationRepository {
  // TODO: findById(id: string): Promise<Application | null>
  // TODO: findByUser(userId: string): Promise<Application[]>
  // TODO: create(app: Partial<Application>): Promise<Application | null>
  // TODO: update(id: string, updates: Partial<Application>): Promise<Application | null>
  // TODO: delete(id: string): Promise<void>
}
