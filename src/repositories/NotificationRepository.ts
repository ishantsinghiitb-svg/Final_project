// Placeholder — methods will be implemented in Phase 3+
export class NotificationRepository {
  // TODO: findByUser(userId: string): Promise<Notification[]>
  // TODO: markRead(id: string): Promise<void>
  // TODO: markAllRead(userId: string): Promise<void>
  // TODO: create(notif: Partial<Notification>): Promise<Notification | null>
}
