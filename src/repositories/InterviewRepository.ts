// Placeholder — methods will be implemented in Phase 3+
export class InterviewRepository {
  // TODO: findById(id: string): Promise<Interview | null>
  // TODO: findByUser(userId: string): Promise<Interview[]>
  // TODO: create(interview: Partial<Interview>): Promise<Interview | null>
  // TODO: update(id: string, updates: Partial<Interview>): Promise<Interview | null>
  // TODO: delete(id: string): Promise<void>
}
