// Placeholder — methods will be implemented in Phase 3+
export class ExtensionRepository {
  // TODO: saveJob(userId: string, jobData: unknown): Promise<void>
  // TODO: syncBookmarks(userId: string): Promise<void>
}
