// Placeholder — methods will be implemented in Phase 3+
export class ResumeRepository {
  // TODO: findById(id: string): Promise<Resume | null>
  // TODO: findByUser(userId: string): Promise<Resume[]>
  // TODO: create(resume: Partial<Resume>): Promise<Resume | null>
  // TODO: update(id: string, updates: Partial<Resume>): Promise<Resume | null>
  // TODO: delete(id: string): Promise<void>
}
