// Placeholder — methods will be implemented in Phase 3+
export class AnalyticsRepository {
  // TODO: getOverview(userId: string): Promise<Analytics>
  // TODO: getActivity(userId: string): Promise<Activity[]>
  // TODO: getWeeklyProgress(userId: string): Promise<{ done: number; goal: number }>
}
